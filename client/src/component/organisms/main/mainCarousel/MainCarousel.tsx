import React, { Component } from "react";
import styles from "../mainCarousel/MainCarousel.module.scss";

type Props = {};

export default function MainCarousel({}: Props) {
  return <div className={styles.carouselWrapper}>MainCarousel</div>;
}
